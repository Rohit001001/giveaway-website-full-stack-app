# Application Environment
NODE_ENV=production

# Database Configuration
DATABASE_URL=file:/app/data/sqlite.db

# Authentication Secret (CHANGE THIS!)
# Generate: openssl rand -base64 32
BETTER_AUTH_SECRET=change-this-to-a-secure-random-string-minimum-32-characters

# Application URL (Update with your EC2 IP or domain)
NEXT_PUBLIC_APP_URL=http://YOUR_EC2_PUBLIC_IP:3000
